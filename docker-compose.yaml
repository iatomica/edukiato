

services:
  # --- Database ---
  # db:
  #   image: postgres:15-alpine
  #   restart: always
  #   ... (comentado para usar solo front con localstorage)

  # --- Backend (API) ---
  # backend:
  #   build: 
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ... (comentado para usar solo front con localstorage)

  # --- Frontend (Web App) ---
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${PORT:-8000}:80"
    # depends_on:
    #   - backend
    networks:
      - edukiato-network

# volumes:
#   postgres_data:

networks:
  edukiato-network:
    driver: bridge